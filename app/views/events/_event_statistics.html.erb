<div class="event-statistics">
  <%= user_review_score_block(event.average_user_review) %>
  <%= pluralize event.num_user_reviews, "User review" %>

  <%= media_review_score_block(event.average_media_review) %>
  <%= pluralize event.num_media_reviews, "Critic review" %>
</div>
